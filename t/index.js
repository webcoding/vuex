import { Store, install } from './store';
import { mapState, mapMutations, mapActions } from './helpers';

export default {
  Store,
  install,
  mapState,
  mapMutations,
  mapActions,
}

export {
  Store,
  install,
  mapState,
  mapMutations,
  mapActions,
}
